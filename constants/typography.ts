/**
 * Inter — use these `fontFamily` values after fonts load in root layout.
 * Loaded weights: see `app/_layout.tsx` (`useFonts`).
 */
export const fonts = {
  regular: 'Inter_400Regular',
  medium: 'Inter_500Medium',
  semiBold: 'Inter_600SemiBold',
  bold: 'Inter_700Bold',
} as const;
